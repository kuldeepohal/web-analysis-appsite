export const metadata = {
  title: 'A2Z Growth Lab | Website Growth Opportunities',
  description: 'Turn website audit findings into prioritized growth actions and estimate the business value of conversion improvements.'
};

export default function GrowthLayout({ children }) {
  return children;
}
